services:
  cassandra:
    image: cassandra:latest
    ports:
      - "9042:9042"
    volumes:
      - ${PWD}/cassandra/data:/var/lib/cassandra
      - ${PWD}/cassandra/scripts:/scripts
    command: ["cassandra", "-f"]
